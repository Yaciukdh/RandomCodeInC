This is a bash shell with limited features. You can cd, ls, and do stuff that regular bash can do. 

Uses execv command.

Has fork(&) and pipe(|) functionality.

Debug variable set in script.

if MYPATH variable not set, will check /bin.

to set MYPATH with multiple directories, separate directories using '#' character.

There are a few other quirks from assignment, can't remember specifics and no longer have criteria.
